{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // No rootDir: this package imports @sotto/database source (a sibling dir),
    // and rootDir would reject files outside ./src. noEmit makes it moot anyway.
    "noEmit": true,
    "paths": {
      "@sotto/database": ["../database/src/index.ts"]
    }
  },
  "include": ["src"]
}
