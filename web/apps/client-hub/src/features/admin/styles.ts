/* Shared Tailwind class strings for the admin forms — one definition so the inputs stay uniform. */

export const inputCls = 'w-full text-sm font-sans border border-border rounded-sm px-3 py-2 bg-bg placeholder:text-text-subtle focus:outline-none focus:border-cosmos-black transition-colors'
